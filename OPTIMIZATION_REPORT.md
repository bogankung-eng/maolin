# 毛邻 V1 优化路线图（第一性原理分析）

> 2026-08-06 · 由产品经理（许清楚）+ 架构师（高见远）双视角，基于 `src/` 真实代码逐行核对产出
> 状态：分析建议稿，尚未实施

---

## 一、第一性原理结论

**从「宠物主人为什么用毛邻」倒推**，核心诉求只有四个：🐾 晒宠记录（情感）· 💊 求助养宠问题（实用）· 💞 社交归属（关系）· 🩺 健康管理（刚需）。

**V1 判断：设计规范 100% 落地，但「社区」是空壳。**
- 唯一真正闭环的是「发布 → Feed → 点赞」；
- **评论、关注、分享、搜索是假交互**（按钮在、功能无）——用户「想互动却无处下脚」；
- **宠物档案这个情感锚点被做成只读列表**（不可点、健康记录全宠混排、无详情页）；
- **健康提醒是被动的**（状态色藏在「我的」页，不主动打扰，用户不会因此回访）。

**工程地基**：TS strict 全开无 any、key 全部稳定、组件分层清晰、Token 变量化 —— 干净。但 **mockApi 是死代码**（UI 绕过它直接改 store，mock→真实后端路径未铺设）、**safe-area 缺失**（iPhone home bar 遮挡底栏）、**无限滚动有竞态**、**persist 无版本迁移**。

> 一句话：V1 该做的「设计规范」都做到了，但产品还没「长出血肉」。

---

## 二、P0 —— 先让 V1 变成「社区」（本轮最该做）

### 产品 P0（补 4 个断点 = 从演示稿变社区）

| # | 建议 | 现状（真实代码佐证） | 优化方向 | 理由 |
|---|------|----------------------|----------|------|
| 1 | **评论体系落地** | `PostDetailPage.tsx:117` 发送按钮无 onClick、评论区「精彩评论加载中…」占位、comments 纯静态数字 | 评论增删、评论数回写 store、楼主可回复 | 社区互动第一入口，不做则详情页是死页 |
| 2 | **关注关系闭环** | `User` 无 `followingIds`、Feed 无关注按钮、关注 Tab 靠 `source` 冒充（`data.ts:139` 仅 2 帖） | 关注按钮（帖头+主页）、用关注表驱动关注流、双向持久化 | 「关注 Tab」现在是假数据，无关系则无归属感 |
| 3 | **宠物详情页** | `PetCard.tsx` 定义了 `onClick` 但 `ProfilePage.tsx:63` 调用时不传 → 卡片不可点 | 健康记录按宠物分组 + 成长时间线 + 该宠物帖子聚合 | 情感锚点，唯一「留住用户看自己宠物」的页面（PRD P1-4） |
| 4 | **通知中心** | 全站无通知入口 | 铃铛入口：被赞/被回复/问题被回答/健康到期 4 类 | 留存回访钩子，无通知则无「回来」的理由 |
| 5 | **兽医认证与回答激励** | `isVet` 静态写死仅 u_zhou、`addAnswer` 恒 false（`useAppStore.ts:128`） | 认证标识 + 「待解答」独立筛选项 + 采纳加分/徽章 | 问答质量是「有用」的根基，认证是差异化壁垒 |
| 6 | **健康提醒主动化** | 状态色需进「我的」才看到、`pet.healthReminder` 静态文案 | 首页/通知横幅提示即将到期宠物、已过期置顶红色 | 从「被动展示」转「主动触发」，健康是高频回访理由 |

### 工程 P0（5 项地基，先修再长肉）

| # | 建议 | 现状（真实代码佐证） | 优化方向 | 理由 |
|---|------|----------------------|----------|------|
| 7 | **mock→真实后端路径铺设** | `mockApi.ts` 的 getFeedPage/insert* 全是死代码，UI 只用 `delay()`，PublishSheet 同步调 store | store action 显式依赖 api 层（mock/fetch 同接口，env 切换） | 全项目最重要的地基：接后端 = 换 service 而非改 store 内部 |
| 8 | **FeedList 无限滚动竞态 + 卸载取消** | `FeedList.tsx:41-53` IO effect 每次重建、闭包 loading 为旧值可并发触发 2 次 loadMore；`BottomSheet.tsx:29-32` 卸载后仍 setState | ref 存 loading 防抖 + mounted ref/AbortController 取消延迟回调 | 消除滚动竞态与内存泄漏 |
| 9 | **persist 版本迁移** | `useAppStore.ts:187` merge 浅合并、无 version，schema 变更直接覆盖丢数据 | 显式 `version:1` + `migrate()` + merge 字段级校验 | 版本升级不丢用户数据 |
| 10 | **测试补盲** | FeedList 无限滚动+下拉刷新**零测试**、Toast 自动隐藏 timer 无测试（34 用例没覆盖核心交互） | 补 FeedList 加载/重置/竞态 + Toast timer 用例 | 核心交互必须进回归网 |
| 11 | **safe-area 适配** | `AppShell.tsx:15`/`QaDetailPage.tsx:143` 硬编码 pb-72px，viewport 已设 `viewport-fit=cover` | 全部补 `pb-[calc(72px+env(safe-area-inset-bottom))]` | iPhone home bar 现在会遮挡底栏与输入条（移动端首屏硬伤） |

---

## 三、P1 —— 增强体验与地基（下轮）

| 维度 | 建议 | 优先级理由 |
|------|------|-----------|
| 功能 | 全局搜索升级（帖/问答/用户/宠物聚合，`TopBar` 现仅内联过滤） | 内容增长后必须有检索 |
| 功能 | 话题/标签体系（帖子 tags 现为死字符串不可点 → 话题页聚合） | 内容组织与发现入口 |
| 功能 | 发帖关联宠物（`PublishSheet.tsx:24` 默认取 pets[0]，多宠用户无法选） | 多宠用户发帖体验错误 |
| 功能 | 图片上传（PRD P2-4，现仅文本；晒宠是核心场景） | 无图伤分享欲 |
| 功能 | 统计数字真实化（`data.ts:13` stats 硬编码，发帖后不变 → 改为派生计算） | 数字造假瓦解信任 |
| 功能 | 提问双输入（现标题=内容前50字截断，`PublishSheet.tsx:50`） | 截断标题是半成品痕迹 |
| 功能 | 同城 LBS 深化（现仅过滤 2 帖 → 医院/约玩/宠物店聚合 + 城市选择） | PRD 承诺的差异化卖点，现在是空壳 |
| 功能 | 分享落地（`PostCard.tsx:87` 分享按钮无动作 → 复制链接/海报） | 死按钮最伤「真实感」 |
| 工程 | 路由懒加载 + vendor 拆分（单 chunk 246KB/gzip 79KB，首屏可降 30%+） | 首屏提速 |
| 工程 | Google Fonts self-host / preload（`index.html:10-15` 外链，国内可访问性存疑） | 首屏关键路径 |
| 工程 | 语义化标签 + 键盘可达（PostCard/QaItem 整卡 div onClick 不可聚焦） | WCAG 基础 |
| 工程 | ProfilePage `window.prompt` 加宠物（`ProfilePage.tsx:27`）→ 小表单弹层 | 移动端体验 |
| 工程 | PostDetail 死按钮（发送无 onClick）→ 实现或 disabled 文案 | 避免虚假承诺 |
| 工程 | 种子日期滚动（`data.ts:81-92` 基于加载时 NOW，跑几个月全过期变红） | 演示数据长期稳定 |
| 工程 | GitHub Actions CI（lint+test+build）+ 静态托管 SPA fallback 文档 | 合入即验证、上线不白屏 |
| 工程 | `src/api/` 接口约定（REST：GET /posts?page、POST /questions/:id/answers） | 平滑迁移后端 |

---

## 四、P2 —— 打磨（后续）

| 维度 | 建议 |
|------|------|
| 产品 | 空态+萌宠插画+行动引导（现全纯文字，冷）· 吉祥物/IP 形象贯穿（现只有颜色记忆点）· 卡片骨架屏 · 关注流缩略卡分层 |
| 产品 | 收藏/稍后看（好物测评沉淀，带货前奏）· 发帖后引导下一步 · 点赞 vibrate 触觉反馈（安卓） |
| 工程 | 深色模式（先收敛 4 处硬编码色再上变量）· store 按领域切片（feed/qa/profile/ui）· computeHealthStatus 移到 `lib/health.ts`（现混在 types）· ID 生成收敛 `lib/id.ts` · Toast 高度 Token 化 |
| 工程 | 多标签页 storage 事件同步 · persist 防抖写 · memo(PostCard)+作者 Map · 清理 vitest timestamp 垃圾文件入 gitignore · mockApi 过期注释 |

---

## 五、推荐执行路线（4 阶段）

```
Phase 1 工程地基（P0-7~11）  →  api层铺设 · 竞态修复 · persist迁移 · 测试补盲 · safe-area
Phase 2 社区血肉（P0-1~6）   →  评论 · 关注 · 宠物详情 · 通知 · 兽医认证 · 健康主动提醒
Phase 3 体验增强（P1）       →  搜索/话题/图片/分享 · 统计真实化 · 懒加载 · CI
Phase 4 打磨（P2）           →  深色模式 · IP吉祥物 · 骨架屏 · store切片 · 动效细节
```

**建议**：Phase 1 是纯工程（风险低、不破坏现有交互）；Phase 2 是让产品「活过来」的关键一跃，做完后毛邻才配叫社区。两阶段可并行推进，互为独立主线。

---

## 六、一句话总结

**V1 的壳是完整的，肉还没长出来**——先花最小代价修掉 5 个工程地基（1~2 天），再补上评论、关注、宠物详情、通知 4 个 P0 产品断点，毛邻就从「演示稿」变成「社区」。
